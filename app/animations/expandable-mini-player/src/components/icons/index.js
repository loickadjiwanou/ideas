import { Ionicons } from '@expo/vector-icons';

export const Home = (props) => (
  <Ionicons {...props} name="home" size={24} />
);

export const Inbox = (props) => (
  <Ionicons {...props} name="mail" size={24} />
);

export const Edit = (props) => (
  <Ionicons {...props} name="create" size={24} />
);

export const Search = (props) => (
  <Ionicons {...props} name="search" size={24} />
);

export const Settings = (props) => (
  <Ionicons {...props} name="settings" size={24} />
);

export const Menu = (props) => (
  <Ionicons {...props} name="ellipsis-horizontal" size={24} />
);

export const Backarrow = (props) => (
  <Ionicons {...props} name="arrow-back" size={24} />
);